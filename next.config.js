/** @type {import('next').NextConfig} */
const nextConfig = {
  // Jika Anda memiliki konfigurasi khusus, bisa ditambahkan di sini.
  // Contoh: reactStrictMode: true,
};

module.exports = nextConfig;